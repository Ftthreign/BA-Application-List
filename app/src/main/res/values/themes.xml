<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BAApplicationlist" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BAApplicationlist" parent="Base.Theme.BAApplicationlist" />
    <style name="Theme.BAApplicationlist.Splash" parent="Theme.Material3.DayNight.NoActionBar"/>
</resources>